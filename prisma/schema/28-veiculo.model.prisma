model Veiculo {
  id             String   @id @default(uuid())
  placaCavalo    String   @unique
  placaCarreta   String?
  motorista      String
  cpfMotorista   String?
  transportadora String?
  cliente        String?

  cte   String?
  nf    String?
  lacre String?

  dataEntrada DateTime
  dataSaida   DateTime?

  status   String?
  situacao String
  filialId String

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Relacionamentos
  entradas Entrada[]
  filial   Filial    @relation(fields: [filialId], references: [id])
  vaga     Vaga?     @relation(fields: [vagaId], references: [id])
  vagaId   Int?

  @@map("Veiculo")
}
